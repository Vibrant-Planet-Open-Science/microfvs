apiVersion: v2
name: microfvs
description: MicroFVS API — a stateless HTTP wrapper around the FVS CLI
type: application
# Never published to a registry: CI renders this chart with `helm template` and commits the
# output to the deployment repository. Version tracks the chart, appVersion the image.
version: 0.1.0
appVersion: "0.0.0"
